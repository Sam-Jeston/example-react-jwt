// Declare all imports that are not imported in a .ts file (.tsx does not count)
import 'react'
import 'react-dom'
import '../materialize/sass/materialize.scss'
import '../materialize/js/bin/materialize.js'
